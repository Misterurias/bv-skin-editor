body {
  background-color: #1a2733;
  color: #ddd;
  font-family: "Arial", sans-serif;
  margin: 0;
  padding: 0;
}

.editor-container {
  display: flex;
  gap: 24px;
  padding: 20px;
  color: #ddd;
}

/* Main content */
.editor-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* Toolbar */
.editor-toolbar {
  margin-bottom: 16px;
  padding: 12px;
  background: rgba(26, 39, 51, 0.85);
  border: 2px solid #009688;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0, 150, 136, 0.3);
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
  overflow-x: auto;
}
.editor-toolbar label {
  font-weight: bold;
}
.editor-btn {
  background: linear-gradient(180deg, #009688, #00796b);
  border: none;
  border-radius: 8px;
  padding: 8px 14px;
  color: #fff;
  font-size: 14px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.2s ease-in-out;
  box-shadow: 0 0 6px rgba(0, 230, 195, 0.4);
}
.editor-btn:hover {
  background: linear-gradient(180deg, #00bfa5, #00897b);
  box-shadow: 0 0 12px rgba(0, 230, 195, 0.8);
  transform: translateY(-1px);
}
.file-input {
  display: none;
}
.file-label {
  background: #009688;
  border-radius: 6px;
  padding: 8px 12px;
  color: #fff;
  cursor: pointer;
  transition: all 0.2s;
}
.file-label:hover {
  background: #00bfa5;
}

/* Canvas */
.editor-canvas {
  width: 700px;
  height: 700px;
  max-width: 90%;
  max-height: 90vh;
  border: 2px solid #009688;
  border-radius: 12px;
  display: block;
  margin: 20px auto;
  background: #0d141c; /* darker contrast outside ball */
  box-shadow: 0 0 20px rgba(0, 230, 195, 0.3);
}

/* Side panels */
.side-panels {
  display: flex;
  flex-direction: column;
  gap: 20px;
  width: 260px;
}

/* Shapes panel */
.shapes-panel {
  background: rgba(26, 39, 51, 0.95);
  border: 2px solid #009688;
  border-radius: 12px;
  padding: 14px;
  box-shadow: 0 0 20px rgba(0, 230, 195, 0.3);
  max-height: 260px;
  overflow-y: auto;
}
.shapes-panel h3 {
  color: #00e6c3;
  margin-bottom: 10px;
  text-align: center;
}
.shape-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
}
.shape-item {
  cursor: pointer;
  text-align: center;
  padding: 6px;
  border-radius: 6px;
  transition: background 0.2s, transform 0.2s;
  background-color: rgba(255, 255, 255, 0.08);
}

.shape-item img {
  filter: brightness(2.5) invert(1);
  transition: filter 0.2s ease;
}

.shape-item:hover {
  background: rgba(0, 230, 195, 0.25);
  transform: scale(1.05);
}

/* Layers panel */
.layers-panel {
  background: rgba(26, 39, 51, 0.95);
  border: 2px solid #009688;
  border-radius: 12px;
  padding: 14px;
  box-shadow: 0 0 20px rgba(0, 230, 195, 0.3);
}
.layers-panel h3 {
  color: #00e6c3;
  font-size: 1.2rem;
  font-weight: bold;
  margin-bottom: 12px;
  text-align: center;
  text-shadow: 0 0 8px rgba(0, 230, 195, 0.6);
}
.layer-btn {
  background: #2a3f52;
  border: 1px solid #009688;
  color: #ddd;
  border-radius: 6px;
  margin: 6px 0;
  margin-right: 15px;
  padding: 8px 12px;
  transition: all 0.2s;
}
.layer-btn:hover {
  background: #345066;
}
.layer-btn.active {
  border: 2px solid #00e6c3;
  background: #1e3146;
  color: #00e6c3;
  font-weight: bold;
}

.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(10, 10, 10, 0.8);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 100;
}

.modal-content {
  background: #121826;
  border: 2px solid #00ffcc;
  border-radius: 12px;
  padding: 24px;
  width: 600px;
  max-height: 80vh;
  overflow-y: auto;
  box-shadow: 0 0 20px rgba(0, 255, 200, 0.3);
  color: #fff;
}

.modal-content h2 {
  text-align: center;
  margin-bottom: 20px;
  color: #00ffcc;
}

.shortcuts-grid {
  display: flex;
  justify-content: space-between;
  gap: 20px;
}

.shortcuts-grid h3 {
  color: #66ffe0;
}

.shortcuts-grid ul {
  list-style: none;
  padding: 0;
}

.shortcuts-grid li {
  margin-bottom: 8px;
  font-size: 14px;
}

.close-btn {
  display: block;
  margin: 20px auto 0;
  background: #00ffcc;
  border: none;
  color: #000;
  padding: 8px 16px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
  transition: background 0.2s ease;
}

.close-btn:hover {
  background: #66ffe0;
}

.shape-properties {
  background: rgba(26, 39, 51, 0.95);
  border: 2px solid #009688;
  border-radius: 12px;
  padding: 14px;
  box-shadow: 0 0 20px rgba(0, 230, 195, 0.3);
}

.shape-properties h3 {
  color: #00e6c3;
  font-size: 1.2rem;
  font-weight: bold;
  margin-bottom: 12px;
  text-align: center;
  text-shadow: 0 0 8px rgba(0, 230, 195, 0.6);
}

.shape-props-form {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.shape-props-form label {
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: #eee;
  font-size: 0.9rem;
}

.shape-props-form input[type="number"] {
  width: 90px;
  background: #1b2b3a;
  border: 1px solid #009688;
  border-radius: 6px;
  color: #fff;
  padding: 4px 6px;
}

.shape-props-form input[type="color"] {
  width: 60px;
  height: 30px;
  border: none;
  border-radius: 6px;
  background: none;
  cursor: pointer;
}

.flip-row {
  display: flex;
  justify-content: space-between;
  margin-top: 5px;
}

.delete-btn {
  background: #b71c1c;
  border: none;
  border-radius: 8px;
  color: #fff;
  padding: 8px 10px;
  cursor: pointer;
  font-weight: bold;
  transition: background 0.2s;
}

.delete-btn:hover {
  background: #d32f2f;
}

.layer-move-row {
  display: flex;
  justify-content: space-between;
  gap: 8px;
  margin-top: 10px;
}

.move-btn {
  flex: 1;
  background: #00695c;
  border: none;
  color: #fff;
  font-weight: bold;
  padding: 6px 10px;
  border-radius: 6px;
  cursor: pointer;
  transition: all 0.2s ease-in-out;
}

.move-btn:hover {
  background: #009688;
  transform: translateY(-1px);
}

.move-btn:disabled {
  background: #2a3f52;
  cursor: not-allowed;
  opacity: 0.5;
}
